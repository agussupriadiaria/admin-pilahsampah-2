SELECT
  *
FROM
  transactions_wd
WHERE
  transactioncode LIKE '%{{table_transactions_wd.searchText}}%'
ORDER BY
  {{table_transactions_wd.sortOrder.column || 'no'}} {{table_transactions_wd.sortOrder.order !== "desc" ? "" : "DESC"}}